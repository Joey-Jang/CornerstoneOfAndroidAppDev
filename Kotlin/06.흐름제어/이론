06. 흐름제어

// if, else if, else

if (조건) {
    동작1
} else if (조건) {
    동작2
} else {
    동작3
}

- if와 else는 1개, else if는 여러개 가능
- else if와 else는 필요하지 않으면 사용하지 않을 수 있음

* when
when (조건을 검사할 값) {
    case1 -> 동작1
    case2 -> 동작2
    case3 -> 동작3
}