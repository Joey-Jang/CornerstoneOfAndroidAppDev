11. 반복문 키워드

for (i in range(1..10) {
    for (j in range(1..10) {
        본문1
        키워드
        본문2
    }
}

1. break
- 반복문 실행 중에 가장 가까운 루프를 탈출
2. continue
- 반복문 실행 중에 가장 가까운 루프의 시작점으로 이동
3. return
- 반복문 실행 중에 가장 가까운 함수를 종료
4. label
- 위 3가지 키워드들의 작용 지점을 설정 (가장 가까운 -> 라벨로 설정한)
- 라벨로 설정할 수 있어야하고, 설정한 라벨을 명시할 수 있어야함